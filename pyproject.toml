[project]
name = "submarino-llamaindex"
version = "0.1.0"
description = "Utilities for building/querying LlamaIndex stores in Submarino"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "llama-index-core>=0.11.14,<0.12",
  "llama-index-embeddings-huggingface>=0.2.3,<0.3",
  "python-dotenv>=1.0.1,<2.0.0",
]

[tool.uv]
dev-dependencies = []
